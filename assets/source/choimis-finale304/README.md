# 최미스 BUILD304 · 망토 휘두르기

내장 `image_gen` 1회 생성 원본을 기계적으로 추출한 6프레임이다. 생성 도구의 실제 모델 ID와 비용은 공개되지 않아 unknown으로 기록한다. 정확한 생성 지시는 `prompt-used.txt`, 불변 원본은 `raw-sheet.png`에 보존했다. 원본 경로는 `/Users/khope@sionic.ai/.codex/generated_images/01a09daa-6644-71a0-a0b4-284c735f3b96/exec-e03ca653-71d2-4073-adc9-85c1eabc83e8.png`이다.

## 런타임 계약

- 파일: `assets/enemies/choimis-cape-swing.png`
- 시트: 448×576 RGBA, 2열×3행, 셀 224×192
- 순서: 좌상0 → 우상1 → 중좌2 → 중우3 → 좌하4 → 우하5
- 동작: 뒤로 모으기 → 높이 들기 → 펼쳐 휘두르기 → 낮게 후속 동작 → 반동 → 복귀
- 공통 피벗: `(136,180)`. y180은 불투명 영역의 exclusive 하단이다.
- 표시 배율: 가로·세로 공통0.714. 기준 대기140px와 같은 화면 키99.96px다.
- 제작 GIF 프레임 시간: 140/110/100/120/140/190ms. GIF 반복은 검수용이며 실제 전투 타이밍은 런타임에서 정한다.

기존 192×192셀·피벗88,180 계약에서는3번 망토가 왼쪽으로45px 넘는다. 몸 축소 없이 전부 보존하기 위해224×192셀·피벗136,180으로 변경했다. 실제 최저 여백은3번 왼쪽3px이며, 모든 프레임은 셀 안에 들어간다.

## 알파·분리·크기

원본은1024×1536 RGBA다. 화면에서 보이는 넓은 색 번짐은 대부분 alpha0 아래의 RGB다. 기존 cape301과 동일하게 alpha128 기준 이진화했으며 색키·팔레트 수정·윤곽선 침식은 없다. 원본의 alpha>0 픽셀455,379개 중 alpha≥128 픽셀355,476개를 보존했다.

균등512px 셀 분할은 중좌 프레임의 망토를 자르므로 사용하지 않았다. alpha128 전경은 정확히6개의 서로 떨어진4방향 연결 성분이며, 각 성분의 모든 픽셀을 손실 없이 격자에 다시 배치했다. 성분 폐기는0개다. 중간 두 포즈의 사각 bbox는 x526~537에서 겹치지만 실제 전경 픽셀은 연결되지 않는다. 좌표별 성분 마스크로 분리했으므로 옆 망토를 섞거나 잘라내지 않는다. 정확한 원본 bbox·면적·재배치 좌표는 `qc-meta.json`에 있다.

`tools/sprites/sheet_processor.py`의 preserve 모드를 재사용한다.0번 머리~신발411px을140px로 맞춘 공통140/411 배율만 적용했다. 프레임별 bbox 재정규화는 없다. 최종 포즈 높이140/137/131/123/142/141px는 몸을 기울이는 자세 차이를 보존한 결과다. 피벗 x는 **bbox의 기하학적 최하단10%**에 있는 신발 픽셀 x범위 중앙을 반올림한다. 전경 픽셀 수의90백분위는3번 낮게 내려온 망토를 신발로 오인하므로 사용하지 않는다. 실제 신발 중앙은136/135.5/136/136/136/135.5px이며 반올림 피벗은 모두136이다.

## 재현·검수

```sh
uv run assets/source/choimis-finale304/export.py
```

`raw-regridded.png`는 연결 성분을 분리한 중간 원본, `processor/`는 공통 처리기의 원본 메타데이터, `sheet-transparent.png`와 `frame-0.png`~`frame-5.png`는 최종 PNG다. `preview-2x.png`, `animation.gif`, `animation-preview-3x.gif`, `idle-comparison-2x.png`로 전체 실루엣과 승인된 대기 자산을 비교할 수 있다. GIF는 포맷 한계에 따라 색상 인덱싱하며 런타임 PNG는 원본 RGB를 최근접 선택만 한다.

검증 결과: 서로 다른 프레임6개, alpha값0/255, 빈 프레임0, 출력 경계 접촉0, 강제 paste clamp0. 처리기 strict QC 통과: body-scale CV0.04212, anchor-y std0.0004883. 재추출 실행과 산출물 크기·피벗·GIF6프레임·런타임/보존 PNG 바이트 일치를 확인했다. Python 규칙 검사 위반0개이며 추출 코드는138 유효 줄이다. 실 게임 연결과 최종 사용자 외형 승인은 별도 단계다.
