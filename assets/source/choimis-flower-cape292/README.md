# 최미스 검은 망토 전투 대기 · cape292

게임 전달: 이 폴더의 `sheet-transparent.png`를 `assets/enemies/choimis-flower-idle.png`로 복사한다. BUILD291의128px셀/64,120 계약은 과거 원본이며 이번 전달은160px셀/80,152이다. 아직 전투 레지스트리 소비자는 없으므로 실제 전투 통합 완료로 간주하지 않는다. 아래의 “게임 미수정”은 스튜디오 제작 단계의 기록이다.

2026-09-22 후속 자산. 기존 `choimis_flower` 전투 대기의 정체성과 손짓을 유지하면서 검은 망토를 어깨에 걸치고 몸 비율을 늘렸다. 흰 머리·검은 눈썹·사각 안경·긴 코·별도 치아 입·분홍 GAP 상하의를 보존한다. 이미지 제작 및 파일 규격 확인 상태이며 사용자 최종 외형 승인, 게임 통합·배포와 구분한다. 기존 게임 PNG, 이동 시트, 런타임 코드는 수정하지 않았다.

- [최종 시트](sheet-transparent.png): 320×320 RGBA, 160×160 셀, 2열×2행.
- 프레임 인덱스: 좌상0 → 우상1 → 좌하2 → 우하3. 왼쪽을 보는 전투 대기다.
- 피벗: 셀 내부 `(80,152)`. 불투명한 발 마지막 행은151이다.
- 재생: `0,1,2,3` 반복; 시간 `300,220,300,300ms`, 한 루프1120ms.
- [투명 GIF](animation.gif), [4배 재생 GIF](animation-preview-4x.gif), [4배 시트](preview-4x.png), [기존/신규 동일 2배 비교](old-vs-new-2x.png).
- `frame-0.png`~`frame-3.png`: 독립160px 프레임. `battle-neutral.png`은 첫 프레임.

최종 몸체 높이는123/122/123/122px, 평균122.5px이다. 기존103/103/102/102px, 평균102.5px보다19.51% 크다. 비교 그림은 두 자산을 동일 픽셀 배율·발선으로 보여 준다. 대략적인 첫 프레임 머리/전체 높이 비율은 기존50.5% → 신규48.0%다. 머리도 조금 커졌으며 머리 크기를 픽셀 단위로 고정한 편집은 아니다. 머리 아래 몸 길이가 더 많이 늘어난 것으로 읽힌다.

## 원본과 재현

`raw-sheet.png`은 내장 이미지 생성 결과(1254×1254, 실제 알파)다. [실제 생성 프롬프트](prompt-used.txt), `old-sheet-reference.png` 및 [후처리 코드](export.py)를 보존한다. 모델의 정확한 ID·비용·내부 품질 설정은 도구에서 공개되지 않아 unknown이다.

```sh
uv run export.py /Users/khope@sionic.ai/Desktop/khope/subtarune
```

기존 게임의 `tools/sprites/sheet_processor.py`를 읽기 전용으로 호출한다. 원본 알파를128 기준으로 이진화하고 실제 배경 투명을 사용한다. 색키·분홍색 제거·팔레트 양자화·새 픽셀 그림은 적용하지 않는다. 공통 배율0.2356321839와 NEAREST로 네 프레임을 추출한 뒤 발 하위10% 픽셀의 가로 중앙값을80, 발 아래 경계를152에 정렬한다. 파일은 이 폴더에만 출력된다.

[최종 수치](qc-meta.json), [처리기 수치](processor/pipeline-meta.json), [시각 검수 기록](qc-report.md)을 함께 확인한다. `processor/`는 중간 중앙 정렬 결과이며 실제 전달 PNG는 폴더 최상단의 `sheet-transparent.png`이다.
