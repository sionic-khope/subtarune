// ─────────────────────────────────────────────────────────────
// 거실 첫 진입 (living.json `enter` → 도착 페이드 인 직후 1회, flag living_entered).
// 형섭이 말하는 줄은 HS()(이름·초상화·형섭 목소리). 사용자 지정 텍스트 그대로, 띄어쓰기/멈춤만 조정.
// ─────────────────────────────────────────────────────────────
const HS = (text, extra = {}) => ({ speaker: '형섭', portrait: 'hyungsub', voice: 'hyungsub', text, ...extra });   // 형섭이 말하는 줄 (인트로 맵)

export const living_enter = Object.assign([
  { wait: 0.5 },
  HS('* 엄마'),
  { wait: 0.3 },
  HS('* 엄마{w=0.6} ???'),
  { wait: 0.4 },
  HS('* 안계시나보다{w=0.4} 코드를 찾아봐야겠네'),
  HS('* 티비쪽에 있으려나{w=0.3} 일단{w=0.4} 아 배고파'),
  HS('* 이것저것 더 챙기면 좋겠다,{w=0.3} 밥상에 뭐 없나...'),
], { silent: true });
