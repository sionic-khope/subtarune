# 전투 사용성 평가 — 2026-09-10 (사용자 "사용성 평가해서 전투 시스템 다시 개편해")

기준: 델타룬 인게임 전투(사용자가 참고로 지정). 평가 축 = **예측 가능성**(다음에 뭐가 오는지 알 수 있나) · **준비 시간**(반응할 틈이 있나) · **가독성**(글자·상태가 보이나) · **조작 일관성**(C/X/방향키가 어디서나 같은가) · **피드백**(내 선택·피격이 소리/그림으로 돌아오나).

## 발견 → 개편
| # | 발견(문제) | 축 | 개편 |
|---|---|---|---|
| 1 | 우리 공격이 끝나면 **탄막이 바로 날아왔다**. 상자가 어디에 생기는지, 소울이 어디 있는지 볼 틈이 없었다 | 준비 시간·예측 | 적 턴 = **준비 단계**(`enemy-prep`): 패널 상자가 그 자리에서 탄막 상자로 펼쳐지고(0.3s) 소울이 나타난다 → 적 말풍선이 한 글자씩 뜬다 → 다 뜬 뒤 **0.9s 준비 시간** → 탄막. 준비 시간 동안 소울을 미리 옮길 수 있다 |
| 2 | 적이 아무 말도 없이 때렸다(내가 적 턴 문구를 지운 뒤). 델타룬은 적 옆 말풍선으로 성격을 보여 주고 그게 "곧 공격" 신호다 | 예측·피드백 | **흰 말풍선**(적 왼쪽, 적 쪽 꼬리, 검은 작은 글씨 12px, 타자) — `enemies.js lines.speak`(1인칭, 다른 적 언급 금지). 탄막이 시작되면 사라짐(상자 위를 가려 탄막을 숨기지 않게) |
| 3 | 적 고르기가 `◀ 레드 CS ▶` 한 줄 — 몇 마리인지, 각자 HP 가 얼마인지 안 보이고 좌우 키가 목록 감각과 안 맞았다 | 가독성·일관성 | 델타룬 FIGHT 처럼 **적 목록**(하트 커서, 이름, HP 바, 숫자) ↑↓. 고르는 적 위 화살표는 유지 |
| 4 | 아이템도 `◀ ▶` 한 줄, 대상도 `◀ ▶` | 일관성 | 아이템 = **2열 격자**(↑↓ 한 칸, ←→ 열), 대상 = **멤버 목록 + 색 HP 바**. 세 화면이 전부 "하트 커서 목록" 한 규칙 |
| 5 | `[공격하기]` 선택 중 글자가 상자보다 커서 잘렸다 | 가독성 | 상자 폭 = 하트 자리 + 글자 폭 + 여백(측정) — 글자가 상자 밖으로 못 나감 |
| 6 | 누구 차례인지 HP 띠의 노란 이름뿐 | 피드백 | 차례인 멤버 스프라이트가 **한 발(10px) 앞으로** 나온다(델타룬) |
| 7 | 전투 진입 때 필드 브금이 0.4s 페이드로 죽고 전투 브금이 로딩되는 사이 **0.5s 공백** | 피드백 | 조우 순간 필드 브금을 끊고(징글만) 전투 브금을 **미리 로드**(`preloadBgm`) → 전투 화면과 동시에 소리 |
| 8 | 승리음이 델타룬 `snd_won` — 사용자가 아는 소리는 언더테일 `snd_victor` | 피드백 | `snd_victor` 로 교체(앞 배치) |
| 9 | HP 가 멤버 카드 안에 있어 화면 중간을 가리고, 회피 중엔 다른 위치에 그려졌다 | 가독성·일관성 | 델타룬식 **하단 HP 띠** 하나로 통일(모든 상태) (앞 배치) |

## 그대로 둔 것(의도)
- 공격은 "생각보다 빠르게"(사용자): 접근 820px/s, 멤버 사이 0.08s.
- 데미지 1 고정·경험치 없음 — 스토리 어드벤처 규칙.
- X = 뒤로가기(이전 멤버로) — 델타룬과 같음.

## 남은 후보(요청 시)
- 적마다 목소리 블립(`def.voice`) — 지금은 나레이션 블립.
- 말풍선이 여러 적일 때 동시에 둘: 지금은 한 마리만 말한다(랜덤).
- 탄막 패턴별 예고(첫 탄 전 0.3s 점멸) — 준비 시간이 생겨서 지금은 불필요.
- 소울 피격 무적 0.75s 동안 깜빡임 강도.

## 검증
- `tests/playtest/battle.mjs`: 잡담 공존, 목록 UI 진입/뒤로가기, **준비 단계(상자 펼침 + 말풍선 + ≥1.1s)** 뒤 탄막, 스크린샷 `battle_02_menu / 02b_target / 03b_prep / 04_bullets`.
- `tests/playtest/teal4.mjs`: 진입 시 필드 브금 끊김 + 전투 브금 250ms 안 재생.
- `tests/unit/enemies.test.mjs`: idle·speak 문구가 다른 적을 언급하지 않음.
