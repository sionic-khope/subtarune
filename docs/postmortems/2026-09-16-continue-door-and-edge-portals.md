# 2026-09-16 이어하기로 문 자리에서 시작하면 안 열림 · 포탈이 한 칸 앞 · QA 신고 대응 (BUILD194)

사용자: “빌드 바뀌고 이어하기 C 하면 적용 안 되는 문제 있는지 체크” → “다음 맵이 안 가져, 여기서 막히잖아”(용암 수로 끝) → “Q 로 가면 갑자기 뚜울라 무대 뒷편이 나오는데” → “포탈을 끝으로 둬야지, 다 도달하기 전에 이동되게 한 블럭 당기냐 — 다른 맵에서는 잘 하다가”.

| # | 증상 | 원인 | 고침 | 재발 방지 |
| --- | --- | --- | --- | --- |
| 1 | 이전 빌드에서 수로 끝 벽에 서 있던 세이브로 새 빌드에서 이어하기 → 그 자리에 생긴 문이 영영 안 열림 | `Trigger.update` 가 겹침을 매 프레임 `inside` 에 적고 **페이드·대사 중에는 발동만 건너뛰었다** → 이어하기 페이드 동안 ‘이미 안에 있음’이 되어 걸어 나갔다 들어오기 전엔 안 밟힘. 벽 끝이라 오른쪽으로는 나갈 수도 없었다 | 밟을 수 없는 상태(페이드·대사)에서는 `inside` 를 적지 않고, 끝나는 순간 발동. 단위 `trigger-inside.test.mjs`, 헤드리스 `continue-save`(문 자리 세이브 → 이어하기 → 바로 넘어감) | **세이브는 어디서든 만들어질 수 있다** — 이전 빌드의 막다른 끝이 다음 빌드의 문 자리다. 문·트리거를 새로 놓으면 “그 자리에서 시작한 세이브”를 반드시 돌려 본다 |
| 2 | 다음 맵으로 넘어가는 자리가 보이는 끝보다 한 칸 앞 | 가장자리 출입구 칸 `H` 를 **바닥처럼 보이지만 막힌** 타일로 두고 문 트리거를 그 안쪽 칸 왼쪽 절반에 놓았다 → 마지막으로 보이는 바닥 칸엔 못 들어가고 그 앞 칸 초입에서 넘어갔다. 다른 맵(무대 홀 오른쪽 등)은 문 칸이 곧 마지막 바닥 칸이라 끝에서 넘어간다 | `H` 는 **걷는 바닥**(맵 밖은 `tileAt` 이 막힌 ' ' 를 돌려줘 떨어질 수 없다), 문 트리거는 `H` 칸의 **맵 끝 쪽 10px**(void10 출구와 같은 방식). 용광로 복도·수로·검사실 1/2 전부. `maps.test`: H 위의 문은 맵 끝(x 0 / pxW)에 닿고 폭 ≤ 16 | **다음 맵으로 넘어가는 자리 = 걸어서 닿는 맵 끝.** 보이는 바닥 칸에 못 들어가게 두지 않는다. 새 출입구를 만들면 끝까지 걸어 넘어가는 프레임을 찍는다 |
| 3 | “Q 로 가면 뚜울라 무대 뒷편이 나온다” | 현재 빌드에서는 재현되지 않음(타이틀 Q → youngcle15/16/lava_raft_top/backstage 모두 맞는 맵). 빌드가 바뀌기 전에 열어 둔 페이지(목록에 새 지점이 없음)에서 끝 근처 항목(`stage_after_show`·`backstage` = youngcle12 대기실)을 고른 것으로 본다 | 헤드리스 `continue-save` 가 타이틀 Q 목록 → youngcle16 을 회귀 검사 | QA 지점을 추가하면 **타이틀 Q 목록으로 실제 골라 들어가는 검사**를 같이 둔다(`?qa=` 만 돌리지 않는다) |
| 4 | (대응) 사용자 스크린샷을 보고 “어느 지점인지 알려 달라”고 되물었다 | 사용자의 세이브 상태(이전 빌드의 끝 벽)로 재현하지 않고 QA 스폰에서만 돌려 “정상”이라 답했다 | #1 재현 → 수정 | **사용자 신고는 사용자의 상태로 재현한다**: 이전 빌드에서 갈 수 있던 가장 끝 자리에서 시작한 세이브가 기본 재현 상태. 되묻기 전에 그걸 먼저 돌린다 |

용어(사용자 2026-09-16): “문이 열린다”(C 로 여는 문)와 “다음 맵으로 바로 이동”(밟으면 넘어감)은 다르다. 용광로 구역의 출입구는 전부 후자(바로 이동)다.
