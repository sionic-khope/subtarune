# 최미스 전투 패턴 B

## 요청과 확정

이 문서는 최미스 보스의 `랩 → 스읍 미스 → 패션` 세 공격 계약이다. 적 체력 200과 파티 피격 피해 15는 enemy 데이터가 소유한다. BUILD297의 실제 영상·음성 랩이 이전 보이스폰트 전용 랩 해석을 대체한다.

## 플레이 규칙

| 패턴 ID | 보이는 단서와 행동 | 공간·시간 규칙 | 성공/실패 |
| --- | --- | --- | --- |
| `choimis_rap` | 최미스가 공격 상자 중앙에서 마이크를 들고 흰 2톤 실루엣으로 랩한다. 지정 영상 22–41초의 실제 움직임을 전장 뒤에 희미하게 유지하고 원음은 기존 보스 BGM 위에 겹친다. `@#$!@#!@#`와 `래퍼딱지를때는중이젠MC로`만 위에서 비처럼 내려온다. | 정확히 19초다. 각 가사는 0.48초 세로 점선 예고 뒤 104px/s로 내려오며 좌·우·중간 열을 바꾼다. 긴 한글은 두 줄로 보이지만 합치면 지정 문자열과 같다. | 중앙 흰 실루엣과 보이는 글자 직사각형만 충돌한다. 한쪽 구석 고정 대신 다음 점선 열을 읽어 좌우로 전환한다. |
| `choimis_seup` | 기존 2.7초 `choimis_seup_miss`를 한 번 재생한다. 들숨 네 파동이 아래 안전→위 안전을 번갈아 만들고, 후반에는 중앙 손가락총 세 줄을 쏜다. | 기존 6.4초와 속도·피해를 유지한다. 파동은 0.35/1.25/2.35/3.55초에 시작하며 각 세 획이 0.18초씩 엇갈린다. 흡입 0.45초, 손가락총 0.55초 예고 뒤 경로가 고정된다. | 네 구석을 모두 지나되 각 파동의 반대 위·아래 통로는 남는다. 다음 파동 전에 안전 통로가 바뀌므로 예고를 따라 전환한다. |
| `choimis_fashion` | “이거 패션어떰?” 뒤 원본 시트의 표범 정장, 지브라 코트, 체크 아이돌복, 하트별 점프수트가 순서대로 런웨이처럼 교차한다. | 기존 7.5초와 속도·피해를 유지한다. 0.35/1.55/2.65/4.00초의 불균등 시차와 아래/위 교대 통로를 쓰며, 각 원본 알파 bbox를 비율 유지한 48×60px 안에 그린다. | 생성된 흰 막대나 외곽선을 덧씌우지 않는다. 실제 불투명 의상 픽셀만 충돌하며 다음 의상마다 반대 통로로 이동한다. DOM 없는 검사에서는 결정적인 전용 기하 폴백을 쓴다. |

## 원문과 오디오

- 랩 영상: `https://www.youtube.com/watch?v=LHT3XMBGO_8`의 22–41초. 런타임 파일은 `assets/video/choimis-forever-22-41.mp4`다.
- 랩 가사 탄 원문은 `@#$!@#!@#`, `래퍼딱지를때는중이젠MC로` 두 문자열뿐이다.
- 스읍 말풍선 원문: `스읍 미스`.
- 패션 말풍선 원문: `이거 패션어떰?`.
- `choimis_seup`만 기존 `assets/audio/sfx/choimis_seup_miss.mp3`를 패턴 시작에 한 번 재생한다. 탄마다 효과음을 재생하지 않는다.
- 랩 영상 오디오는 음소거하지 않으며 기존 BGM을 멈추거나 다시 시작하지 않는다. 게임 사운드 설정, 숨김 탭, 패턴 시계에 동기화한다.

## 자산과 연동

- 패턴 모듈: `src/battle/choimis-patterns-b.js`, export `CHOIMIS_PATTERNS_B`.
- 영상 모듈: `src/battle/choimis-rap-video.js`. 전투 진입 중 최대 3초만 디코드를 기다리고, 실패·재생 거부는 경고 상태로 남긴다. 정상 종료와 패배·재도전·취소·승리에서 요소·소스를 해제한다.
- 영상 소스/인코딩 원본: `assets/source/choimis-rap297/README.md`, `metadata.json`. 출력은 480×270,15fps,H.264 Baseline+A​AC,19.000초,1,043,724바이트다.
- enemy projectile key `mic`: `assets/enemies/choimis-rap.png`, 320×320 RGBA, 160셀 2×2 행우선 프레임. 승인된 body bbox 123px를 공통 enemy scale 0.506과 pivot `[80,152]`로 그려 보이는 몸 높이를 62.238px로 맞춘다. 상자 안에서는 `monoPortrait` 흰 2톤으로 변환하고 같은 62px 충돌 실루엣으로 잘라 쓴다.
- enemy projectile key `fashion`: `assets/props/choimis-fashion.png`, 192×192 RGBA, 96셀 2×2 행우선 프레임. 표범·지브라·체커 아이돌·하트별 점프수트 순서로 쓴다. 이미지마다 프레임 알파 마스크를 한 번 캐시해 렌더 crop과 충돌에 같은 bbox를 사용한다. 자산이 없을 때만 결정적 분홍 의상 도트 폴백을 쓴다.
- 공유 등록은 `src/battle/bullets.js`의 `...CHOIMIS_PATTERNS_B` 한 줄이며 새 전역 Bullet shape가 필요 없다.

## 완료 판정

`tests/unit/choimis-patterns-b.test.mjs`가 registry ID,19초/두 가사/하강 방향,CJK 글꼴과 상자 여백,영상 재생·mute/pause/숨김 탭·정리,실파일 계약,최소 0.3초 예고,교대 통로,네 의상 실루엣,네 구석 압박과 60Hz 도달 가능 경로를 검증한다. 실제 전투 검수는 영상 움직임·원음+BGM 중첩·예고·활성·종료 프레임과 패턴 종료 뒤 꽃망토 복귀를 별도로 캡처한다.
