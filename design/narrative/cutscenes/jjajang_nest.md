# 드럼통 둥지 (BUILD249, 2026-09-19)

## 사용자 브리핑 (원문)

> 그 다음맵 만들건데, 다음맵으로 가면 오른쪽길 1초정도 걷다가 뭔가 둥글게 둥지처럼 지어져있고 뭔가 드럼통 수십개 쌓여있는 스프라이트, 드럼통들이 쓰레기장처럼 널브러져있는 적당히큰 동그라미 공간 보스전 맵 느낌으로 맵 찍어줄래 먼저
>
> 그리고 브금이 꺼져야함.
>
> 가리고 딱 맵 중간 살짝 오른쪽에 상호작용할수있는 드럼통 하나 배치해주고

## 구현

| 원문 | 구현 | 값 |
|---|---|---|
| 다음맵, 오른쪽길 1초정도 걷다가 | `tools/maps/jjajang_nest.py` 30×26: 굽은 물길(jjajang_bend2) 오른쪽 문(새로 둠) → 왼쪽 가장자리(12~13행)로 들어와 오른쪽으로 6칸. 스폰에서 원 경계까지 152px ≈ 1.3초(걷기 120px/s), 플레이테스트 실측 0.8초 | `ENTRY_COLS` |
| 둥글게 둥지처럼, 적당히 큰 동그라미 공간 | 중심 (15,13) 반지름 9칸의 원형 바닥(지름 576px). 화면(480)보다 조금 커서 둘러싼 드럼통 벽이 화면에 걸쳐 보인다 | `meta.nest` |
| 드럼통 수십개 쌓여있는 스프라이트 | 새 소품 두 종(gpt-image, `assets/source/jjajang-nest-v1`): 큰 더미 `jjajang_drum_pile_big.png`(드럼통 15개쯤 쌓임, 111×86), 작은 더미 `jjajang_drum_pile_small.png`(5개쯤 널브러짐, 102×48). 녹슨 자국 포함 | |
| 쓰레기장처럼 널브러져있는 | 둘레에 20등분으로 큰·작은 더미를 번갈아 두르고(17개), 공간 안쪽 가장자리에도 5개를 널브러뜨린다. 가운데는 비워 둔다(보스전 무대) | |
| 보스전 맵 느낌 | 출구는 왼쪽 문 하나. 오른쪽·위·아래는 드럼통 벽과 숲으로 막힌 닫힌 무대 | |
| 브금이 꺼져야함 | 맵 `bgm: null` + `JJAJANG_AFTER_JOIN_MAPS` 에 넣지 않음 → `changeMap` 이 `stopBgm(0.4)`. 플레이테스트가 굽은 물길(my_castle_town) → 둥지(무음)를 확인 | |
| 맵 중간 살짝 오른쪽에 상호작용 드럼통 하나 | 중심에서 오른쪽 4칸(19열, 맵 가운데 15보다 오른쪽)에 `assets/props/jjajang_drum.png` 하나, `script: jjajang_nest_drum`(막힘) → 옆에서 C | |

연출·대사는 아직 브리핑이 없어 `jjajang_nest_drum` 은 나레이션 한 줄(“* 드럼통이다.”)만 둔다. 브리핑이 오면 이 스크립트를 그 내용으로 바꾼다.

QA `jjajang_nest`(입구), `jjajang_nest_center`(드럼통 앞). 검사: `tests/unit/jjajang-nest.test.mjs`, `tests/playtest/jjajang-nest.mjs`(브금 꺼짐 → 1초 걷기 → 더미 → 드럼통 C).
