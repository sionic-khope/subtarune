# 섭타룬 렌더링 기준

## 1. 정체성

기존 저해상도 픽셀 게임을 보존한다. 새 웹 UI나 그림체로 재설계하지 않는다. 캐릭터 원본과 설정의 기준은 이미지 스튜디오 README다. 형섭은 모든 방향에서 입이 없다.

## 2. 색

캐릭터 색은 `assets/sprites/` PNG, 대체 도트는 `src/data/art.js` PALETTES가 기준이다. 필터·색 양자화·외곽 침식 없이 보존한다. 페이지 바탕 `#0b0b10`, 캔버스 `#000`, 기본 글자 `#cfcfdd`는 `css/style.css`의 기존 값이다.

## 3. 글꼴

`src/ui/font.js`의 FONT_PRESET과 F를 사용한다. 현재 NeoDunggeunmo 16px, 행간 18px, 나레이션 행간 22px. 대화창 렌더링과 초상화 규격은 이번 보행 수정에서 변경하지 않는다.

## 4. 공간과 확대

`world.js`의 SCREEN_W/H = 480×360, RENDER_SCALE = 2가 논리/캔버스 해상도 기준이다. CHAR_SCALE = 1.43은 기존 사용자 지정 크기이므로 유지한다. 캐릭터 좌표는 반올림하며 Canvas 보간은 끈다.

화면에 캔버스가 들어갈 때는 캔버스 한 픽셀을 디스플레이의 정수 개 픽셀로 표시한다. 최대 정수 배율을 선택하고 남는 공간은 레터박스로 둔다. 원본 캔버스조차 들어가지 않는 작은 화면은 전체 게임이 보이는 비율 축소를 우선한다. 이때 픽셀 손실은 피할 수 없다. 캐릭터 자체의 1.43배에 따른 비균등 픽셀은 별도 아트 재작업 없이 완전히 제거할 수 없다.

## 5. 재사용 구성

- 전투 모션 미리보기: 기존 Canvas 위에 검정 배경과 기본 FONT로 세 캐릭터를 나란히 표시한다. 좌우 선택, C 공격, X 복귀는 기존 Input 추상화를 사용한다. 대기/공격/복귀/이미지 로드 실패 상태를 명확히 구분한다. 체력·적·전투 결과는 없다.
- 전투 아틀라스: 1536×1024 생성 원본, 대기 4장+공격 4장. 프레임별 소스 사각형과 발 기준점을 데이터로 보관하여 칼/도끼/마법 효과를 자르지 않는다. 1/4배 화면 표시를 기준으로 하며 원본 색과 계단 윤곽을 유지한다. 생성된 단색 마젠타(#ff00ff) 배경만 로드 시 투명화하고 결과를 캐시한다.

- `characterSprite`: 원본 시트 또는 도트 팔레트를 한 번 캐시한다. 4방향, 정지/이동/누운 상태를 공유한다.
- `Character.drawSprite`: 발 기준 위치, 그림자, 카메라 좌표를 적용한다.
- `#stage`/`#screen`: 단일 게임 화면을 중앙 배치한다. 별도 스크롤 UI가 없다.
- `?map=test&sprite=hyungsub`: 실제 엔진의 캐릭터 상태 검증 화면. 다른 등록 캐릭터도 같은 경로로 검사한다.

## 6. 움직임

전투 대기는 4프레임 반복, 공격은 예비 동작→타격/시전→후속 동작→복귀 한 번 재생 후 대기로 돌아간다. 재생 중 입력으로 공격을 계속 재시작하지 않는다. 시간과 프레임·발 기준점은 전투 아틀라스 데이터가 기준이다. 신규 게임 규칙이나 스토리 이벤트를 추가하지 않는다.

옆걸음은 중립 → 발 A → 중립 → 발 B의 3개 고유 포즈, 4개 시간 슬롯이다. 형섭은 기존 하체 원본 포즈를 재사용하여 다리가 교차하도록 다듬고 입 없는 상체는 고정한다. 다른 캐릭터는 기존 3포즈를 유지한다. 프레임 진행은 이동 중에만, 정지는 중립으로 돌아간다. 이동 속도·충돌·걷기/달리기 입력은 바꾸지 않는다. React micro-interaction 카탈로그가 아닌 기존 게임 프레임 애니메이션을 사용한다.

## 7. 표면

PNG 알파와 기존 그림자만 사용한다. 선명도를 이유로 블러·샤픈·추가 명암을 덧씌우지 않는다.

### 바론 둥지 연출 (2026-09-12)

obj3은 기존 오브제 지역의 얕은 물길·초록 나무·숲 원경을 잇는 짧은 북쪽 진입로다. obj4는 같은 지역 안의 원형 둥지이며 안쪽은 전용 젖은 회청색 바위 바닥, 바깥은 얕은 물이다. 옛 보라 허공의 바닥·절벽·불 원경은 사용하지 않는다. obj4의 `spotlight`는 맵 좌표 중심·타원 반경·빛 강도를 소유한다. 외곽 `dim:0.16`, 중앙 라일락 빛 RGB(220,202,255) alpha0.14에서 가장자리0으로 감쇠한다. 어둠은 중심0에서 타원 바깥0.16으로 증가한다. UI는 이 조명에 영향을 받지 않는다.

붉은·보라 가시 PNG를 반경548×446의 둘레에 배치하고 남쪽 입구는 비운다. 바론의 개별 `visualScale:1.8`과 전체 월드 `zoom:0.7`을 함께 사용해 큰 머리·전신과 뒤쪽 붉은 가시를 보여 준다. 대화는 하단 인물 샷, 포효는 대화창 없는 넓은 샷, 대포는 `zoom:2.6` 클로즈업으로 구분한다. 진동 뒤 바론은 0.28초 ease-out으로 분출하며 새 보라 돌풍 FX를 한 번 재생한다. 새 UI·React 동작은 추가하지 않고 기존 Canvas/컷신 노드를 재사용한다.

## 8. 접근성과 범위

### 바론 전투 추가 (2026-09-12)

기존 전투 UI·폰트·하단 HP 띠와 480×360 좌표계를 유지한다. `baron_nest` 배경은 obj4의 최신 젖은 바닥(E)·얕은 물(a)·붉은 가시 PNG를 한 번 캐시하여 사용하고 검정 0.45 겹으로 탄과 캐릭터 대비를 확보한다. 기존 사원/보라숲 배경은 재사용하지 않는다. 보스 원본 256px 셀은 보존하고 배치 배율0.9·발(348,222)로 사각형(233,8)~(463,238)을 패널 위에 넣는다. 일반 공격/아이템·방향키 회피·게임패드 Input 계약을 유지하며 색 외에도 영역 예고와 탄 모양으로 위험을 구별한다. 바론은 3초 준비 진동 후 0.28초 분출한다. 이후 기믹은 기존 공격/적 턴 모드 등록 계약을 사용한다. 전체 웹사이트 감사나 새 접근성 기능은 이 전투 추가 범위가 아니다.

키보드 이동 및 정지 피드백을 유지하고 375/768/1280px에서 화면 전체가 보이는지 확인한다. 이번 작업은 보행/표시 개선이며 기존 Canvas 게임의 스크린리더 대응, 터치 조작, 전체 사이트 SEO 감사는 포함하지 않는다. 그런 기능이 검증되었다고 주장하지 않는다.
